<Window x:Class="SytexLCore.TranslationBubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        SizeToContent="WidthAndHeight" MaxWidth="450" MinWidth="250"
        WindowStartupLocation="Manual"
        Title="SYTEX L-Core Çeviri Balonu"
        Loaded="Window_Loaded" MouseDown="Window_MouseDown">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.25"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder" Margin="10" Padding="16,12" CornerRadius="12" Background="#F2080B11" BorderBrush="#00F5FF" BorderThickness="1.5">
        <Border.Effect>
            <DropShadowEffect Color="#00F5FF" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık ve Kapatma Butonu -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="✦ SYTEX MANUAL SCAN" Foreground="#00F5FF" FontSize="10" FontFamily="Segoe UI" FontWeight="Bold"/>
                </StackPanel>
                
                <Button Grid.Column="1" Width="20" Height="20" Content="×" Foreground="#FFFFFF" Background="Transparent" BorderBrush="Transparent" Click="CloseBtn_Click" FontSize="16" FontFamily="Segoe UI" FontWeight="Bold" VerticalAlignment="Center" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#FF0055"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Yükleniyor Paneli -->
            <StackPanel x:Name="LoadingPanel" Grid.Row="1" Margin="0,10" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="180" Height="4" Background="#22FFFFFF" Foreground="#00F5FF" BorderThickness="0"/>
                <TextBlock Text="OCR taranıyor ve çevriliyor..." Foreground="#88FFFFFF" FontSize="11" Margin="0,8,0,0" HorizontalAlignment="Center" FontFamily="Segoe UI"/>
            </StackPanel>

            <!-- Sonuç Paneli -->
            <StackPanel x:Name="ResultPanel" Grid.Row="2" Visibility="Collapsed">
                <!-- Orijinal Metin (Küçük ve Eğik) -->
                <TextBlock x:Name="OriginalTxt" Foreground="#88FFFFFF" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8" FontStyle="Italic" FontFamily="Segoe UI"/>
                
                <!-- Çevrilmiş Metin (Büyük ve Net) -->
                <TextBlock x:Name="TranslatedTxt" Foreground="#FFFFFF" FontSize="14" TextWrapping="Wrap" FontWeight="Medium" LineHeight="20" FontFamily="Segoe UI"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
